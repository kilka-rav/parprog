TEST1: Наивная реализация перемножения матриц. 
TEST2: Оптимизированная версия
TEST3: Оптимизированая версия + многопоточность для подсчета матричных выражений, например, A*B + C*D + E*G + F*H
TEST4: Оптимизированная версия + многопоточность(Разделение матрицы на 4 блока и подсчет этих матриц отдельными тредами)

RUN:
__________________
make -f Makefile
./speed < test6.txt
__________________
P.S. В текстовом файле данные о размерах матриц, матрицы заполняются рандомно в диапазоне от (0, 10). Результаты тестов находятся в файлах OUT_TEST. Данные о тестирование на моем ПК находятся в result.png
Оптимизации:
Добавил loop unrolling, перестановку циклов, вынес переменные за тело цикла. Добавил threads.
